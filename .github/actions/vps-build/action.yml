name: vps-build
description: Configure, compile, and install AzerothCore to ACORE_STAGING on the VPS runner
runs:
  using: composite
  steps:
    - name: Configure, compile, and install
      shell: bash
      run: bash "${GITHUB_WORKSPACE}/apps/deploy/debian12/vps-compile-install.sh"
