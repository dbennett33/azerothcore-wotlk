name: branch-protection

# PRs to Playerbot must come from dev only. Add job "Validate PR source" as a required
# status check on the Playerbot branch ruleset (Settings → Rules).

on:
  pull_request:
    branches:
      - Playerbot
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review

jobs:
  validate-pr-source:
    name: Validate PR source
    runs-on: ubuntu-latest
    steps:
      - name: PR must come from dev
        run: |
          echo "PR from: ${{ github.head_ref }} → ${{ github.base_ref }}"
          if [ "${{ github.head_ref }}" != "dev" ]; then
            echo "::error::PRs to Playerbot must come from dev only (got: ${{ github.head_ref }})"
            exit 1
          fi
          echo "OK: dev → Playerbot"
