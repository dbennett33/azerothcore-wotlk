# Coordinates

Read `.agents/skills/wow-coordinates/SKILL.md` before any `.go`, SOAP
teleport, `TeleportTo`, or placing a spawn by xyz. Visual walks of a custom
instance: `.agents/skills/walk-instance/SKILL.md`. This file does not
duplicate those rules.
