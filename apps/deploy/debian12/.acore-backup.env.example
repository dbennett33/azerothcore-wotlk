# Copy to /home/acore/.acore-backup.env on the VPS (never commit the real file).
MYSQL_USER=acore
MYSQL_PASS=CHANGE_ME
MYSQL_HOST=127.0.0.1

# Optional: SOAP GM account for deploy warnings (sec level 3 / Administrator).
# SOAP_USER=deploybot
# SOAP_PASS=CHANGE_ME
